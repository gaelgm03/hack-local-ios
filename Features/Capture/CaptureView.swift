import SwiftUI

/// Camera preview + optional voice/text input for context capture.
struct CaptureView: View {
    var body: some View {
        // TODO: Camera preview, text field, voice button, skip button
        Text("Capture Context")
    }
}

#Preview {
    CaptureView()
}
