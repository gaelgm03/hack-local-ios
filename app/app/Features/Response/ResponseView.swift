import SwiftUI

/// Displays the AI's empathetic message and CTA to start an intervention.
struct ResponseView: View {
    var body: some View {
        // TODO: Empathetic message in large type + "Respira conmigo 30s" button
        Text("AI Response")
    }
}

#Preview {
    ResponseView()
}
