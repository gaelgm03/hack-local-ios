import SwiftUI

/// Post-session check-in: "¿Cómo te sientes ahora?" with 3-emoji scale.
struct CheckInView: View {
    var body: some View {
        // TODO: 3 emoji buttons (worse, same, better) + gentle confirmation
        Text("Check In")
    }
}

#Preview {
    CheckInView()
}
