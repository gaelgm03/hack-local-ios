import SwiftUI

/// Animated breathing session: expanding/contracting orb with haptics and timer.
struct BreathingSessionView: View {
    var body: some View {
        // TODO: Animated orb, inhale/exhale labels, timer, skip button
        Text("Breathing Session")
    }
}

#Preview {
    BreathingSessionView()
}
