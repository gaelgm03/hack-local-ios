import SwiftUI

/// 5-4-3-2-1 grounding exercise: step-by-step sensory prompts.
struct GroundingSessionView: View {
    var body: some View {
        // TODO: Step cards for each sense (see, touch, hear, smell, taste)
        Text("Grounding Session")
    }
}

#Preview {
    GroundingSessionView()
}
