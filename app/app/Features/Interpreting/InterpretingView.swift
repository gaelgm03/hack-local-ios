import SwiftUI

/// Loading screen while AI processes context. Shows pulsing orb.
struct InterpretingView: View {
    var body: some View {
        // TODO: Pulsing orb + "Entendiendo tu momento..." subtitle
        Text("Interpretando...")
    }
}

#Preview {
    InterpretingView()
}
