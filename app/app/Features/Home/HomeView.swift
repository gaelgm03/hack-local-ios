import SwiftUI

struct HomeView: View {
    // TODO: Add @State var viewModel = HomeViewModel()

    var body: some View {
        // TODO: Replace with real home screen layout
        Text("Calmly")
            .font(.largeTitle)
    }
}

#Preview {
    HomeView()
}
