import Foundation

/// Copy this file to APIConfig.swift and fill in your API key.
/// APIConfig.swift is gitignored — never commit real keys.
enum APIConfigExample {
    static let openAIKey = "sk-your-key-here"
    static let baseURL = "https://api.openai.com/v1"
    static let model = "gpt-4o-mini"
}
